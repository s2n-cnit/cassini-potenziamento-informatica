

# -----------------------
# ./Baglietto-Luca - Luca Baglietto/020.py
#
def concatena():
    while True:
        s = input("Input: ")
        if s == '':
            break
        print(s, end=' ')
    print()
concatena()

# -----------------------
# ./roggero_costantino/020.py
#
def pr():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
pr()

# -----------------------
# ./lin-matteo - Matth6ew/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

user_inputs = get_user_inputs()

print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./molinari-giorgia/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

# Chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

# Stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./porcile-filippo.zip - Filippo Porcile/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

# Chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

# Stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./perkola_klaudia - Klaudia Perkola/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

#chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

#stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./ExamFrittoli - Lorenzo Frittoli/020.py
#
def pr():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
pr()

# -----------------------
# ./alessandro-lugaro/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

user_inputs = get_user_inputs()

print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./botta-alessio - Alessio Botta/020.py
#
def pr():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
pr()

# -----------------------
# ./esercizi informatica minetti giulio - Giulio Minetti/020.py
#
def pr():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
pr()

# -----------------------
# ./Surdich-Lorenzo - Lorenzo Surdich/020.py
#
def input_senza_andare_a_capo():
    inputs = []

    while True:
        user_input = input()
        if user_input == '':
            break  
        inputs.append(user_input)

    print(''.join(inputs), end='')

input_senza_andare_a_capo()

# -----------------------
# ./portella-francesco - Francesco Portella/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

user_inputs = get_user_inputs()

print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./moretti-christian/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

# Chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

# Stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./zumiani-sara.zip - Sara Zumiani/020.py
#
def pr():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
pr()

# -----------------------
# ./hu-xuan-di/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

# Chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

# Stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./Hunag-Marco.zip - Marco Huang/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

# Chiamata alla funzione per ottenere gli input dall'utente
user_inputs = get_user_inputs()

# Stampa gli input ottenuti senza andare a capo
print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./zono veran - Fabrizio Verani/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

user_inputs = get_user_inputs()

print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")

# -----------------------
# ./galvani-vezzi-lidia.zip - Lidia Galvani Vezzi/020.py
#
def print():
    yay = True
    while yay:
        i = input()
        if i != '':
            print(i)
        else:
            yay = False
            
print()

# -----------------------
# ./Matteo-Marazzi/020.py
#
def get_user_inputs():
    inputs = []
    while True:
        user_input = input("Inserisci un input (INVIO per terminare): ")
        if user_input == "":
            break
        inputs.append(user_input)
    return inputs

user_inputs = get_user_inputs()

print("Gli input dell'utente sono:", end=" ")
for input_value in user_inputs:
    print(input_value, end=" ")