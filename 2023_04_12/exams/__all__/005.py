

# -----------------------
# ./Baglietto-Luca - Luca Baglietto/005.py
#
if __name__ == '__main__':
    numbers = input("Inserisci una lista di numeri: ").split()
    sm = 0
    for n in numbers:
        sm += int(n)
    print("La somma è", sm)


# -----------------------
# ./roggero_costantino/005.py
#
lista_numeri = [10, 20, 30, 40, 50]

somma = 0

for numero in lista_numeri:
    somma += numero

print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./lin-matteo - Matth6ew/005.py
#
def main():
    inputnum = input('inserire lista numeri:')
    lista = inputnum.split()
    sum = 0
    for i in range(len(lista)):
        sum = sum + float(lista[i])
    print(sum)

main()

# -----------------------
# ./Zanardi-Leonardo.zip - leonardo zanardi/005.py
#
def sum_list():
    numeri = list(map(float, input('List: ').strip().split(',')))
    risultato = 0
    for n in numeri:
        risultato += n
        
    print(risultato)
    
sum_list()

# -----------------------
# ./molinari-giorgia/005.py
#
# Definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

# Inizializza una variabile per la somma
somma = 0

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Aggiungi il numero corrente alla somma
    somma += numero

# Stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./porcile-filippo.zip - Filippo Porcile/005.py
#
# Definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

# Inizializza una variabile per la somma
somma = 0

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Aggiungi il numero corrente alla somma
    somma += numero

# Stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./perkola_klaudia - Klaudia Perkola/005.py
#
#definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

#inizializza una variabile per la somma
somma = 0

#utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    #aggiungi il numero corrente alla somma
    somma += numero

#stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./ExamFrittoli - Lorenzo Frittoli/005.py
#
def sum_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
sum_list()

# -----------------------
# ./alessandro-lugaro/005.py
#
listanumeri = [15, 18, 22, 35, 44]

somma = 0

for numero in listanumeri:
    somma += numero

print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./botta-alessio - Alessio Botta/005.py
#
def sum_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
sum_list()

# -----------------------
# ./esercizi informatica minetti giulio - Giulio Minetti/005.py
#
def sum_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
sum_list()

# -----------------------
# ./Surdich-Lorenzo - Lorenzo Surdich/005.py
#
numeri = input("Inserisci una lista di numeri separati da uno spazio: ").split()
somma = 0

for numero in numeri:
    somma += int(numero)

print("La somma di tutti i numeri è:", somma)

# -----------------------
# ./todde-lorenzo - lorenzo todde/005.py
#
def main():
    numbers = input("inserisci i numeri che vuoi sommare: ")
    numbers_list = numbers.split()
    sumtot = 0
    for i in numbers_list:
        sumtot = sumtot + float(i)
    print(sumtot)

main()



# -----------------------
# ./dapino-berenice/005.py
#
def main(): 
    list = [2, 3, 6, 8, 1 ]
    num = 0
    for i in list: 
        num = num + i 
    
    print("Sum: ", num)

main()

# -----------------------
# ./portella-francesco - Francesco Portella/005.py
#
def main():
    inputnum = input('inserire lista numeri:')
    lista = inputnum.split()
    sum = 0
    for i in range(len(lista)):
        sum = sum + float(lista[i])
    print(sum)

main()

# -----------------------
# ./moretti-christian/005.py
#
# Definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

# Inizializza una variabile per la somma
somma = 0

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Aggiungi il numero corrente alla somma
    somma += numero

# Stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./zumiani-sara.zip - Sara Zumiani/005.py
#
def sum_l():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
sum_l()

# -----------------------
# ./hu-xuan-di/005.py
#
# Definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

# Inizializza una variabile per la somma
somma = 0

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Aggiungi il numero corrente alla somma
    somma += numero

# Stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./Hunag-Marco.zip - Marco Huang/005.py
#
# Definisci la lista di numeri
lista_numeri = [10, 20, 30, 40, 50]

# Inizializza una variabile per la somma
somma = 0

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Aggiungi il numero corrente alla somma
    somma += numero

# Stampa la somma a schermo
print("La somma di tutti gli elementi nella lista è:", somma)

# -----------------------
# ./zono veran - Fabrizio Verani/005.py
#
def sum_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
sum_list()

# -----------------------
# ./galvani-vezzi-lidia.zip - Lidia Galvani Vezzi/005.py
#
def somma():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 0
    for n in nums:
        total += n
        
    print(total)
    
somma()

# -----------------------
# ./Matteo-Marazzi/005.py
#
listanumeri = [15, 18, 22, 35, 44]

somma = 0

for numero in listanumeri:
    somma += numero

print("La somma di tutti gli elementi nella lista è:", somma)