

# -----------------------
# ./Baglietto-Luca - Luca Baglietto/016.py
#
import platform

def os_info():
    return platform.platform()


# -----------------------
# ./roggero_costantino/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./lin-matteo - Matth6ew/016.py
#
import platform

def get_os_info():
    os_name = platform.system()
    os_release = platform.release()

    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

os_info = get_os_info()
print(os_info)

# -----------------------
# ./Zanardi-Leonardo.zip - leonardo zanardi/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./molinari-giorgia/016.py
#
import platform

def get_os_info():
    # Ottiene il nome del Sistema Operativo
    os_name = platform.system()
    # Ottiene la release del Sistema Operativo
    os_release = platform.release()

    # Restituisce una stringa formattata con le informazioni ottenute
    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

# Esempio di utilizzo della funzione
os_info = get_os_info()
print(os_info)

# -----------------------
# ./porcile-filippo.zip - Filippo Porcile/016.py
#
import platform

def get_os_info():
    # Ottiene il nome del Sistema Operativo
    os_name = platform.system()
    # Ottiene la release del Sistema Operativo
    os_release = platform.release()

    # Restituisce una stringa formattata con le informazioni ottenute
    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

# Esempio di utilizzo della funzione
os_info = get_os_info()
print(os_info)

# -----------------------
# ./perkola_klaudia - Klaudia Perkola/016.py
#
import platform

def sys_info():
    print("Il Sistema attualmente in uso è: " + platform.system())
    print("Info Release: " + platform.release())

# -----------------------
# ./ExamFrittoli - Lorenzo Frittoli/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./alessandro-lugaro/016.py
#
import platform

def get_os_info():
    os_name = platform.system()
    os_release = platform.release()

    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

os_info = get_os_info()
print(os_info)

# -----------------------
# ./botta-alessio - Alessio Botta/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./esercizi informatica minetti giulio - Giulio Minetti/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./Surdich-Lorenzo - Lorenzo Surdich/016.py
#
import platform

def sistema_operativo():
    sistema = platform.system()
    versione = platform.release()
    
    if sistema == "Windows":
        return f"Sistema operativo: Windows {versione}"
    elif sistema == "Linux":
        distro = platform.dist()[0]
        return f"Sistema operativo: {distro} {versione}"
    elif sistema == "Darwin":
        return f"Sistema operativo: macOS {versione}"
    else:
        return "Sistema operativo non riconosciuto"

print(sistema_operativo())

# -----------------------
# ./todde-lorenzo - lorenzo todde/016.py
#
import platform
    
def systemInfo():
    print(platform.system())
    print(platform.release())
    print(platform.version())

systemInfo()

# -----------------------
# ./portella-francesco - Francesco Portella/016.py
#
import platform

def get_os_info():
    os_name = platform.system()
    os_release = platform.release()

    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

os_info = get_os_info()
print(os_info)

# -----------------------
# ./moretti-christian/016.py
#
import platform

def get_os_info():
    # Ottiene il nome del Sistema Operativo
    os_name = platform.system()
    # Ottiene la release del Sistema Operativo
    os_release = platform.release()

    # Restituisce una stringa formattata con le informazioni ottenute
    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

# Esempio di utilizzo della funzione
os_info = get_os_info()
print(os_info)

# -----------------------
# ./zumiani-sara.zip - Sara Zumiani/016.py
#
import platform

def os():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
os()

# -----------------------
# ./hu-xuan-di/016.py
#
import platform

def get_os_info():
    # Ottiene il nome del Sistema Operativo
    os_name = platform.system()
    # Ottiene la release del Sistema Operativo
    os_release = platform.release()

    # Restituisce una stringa formattata con le informazioni ottenute
    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

# Esempio di utilizzo della funzione
os_info = get_os_info()
print(os_info)

# -----------------------
# ./Hunag-Marco.zip - Marco Huang/016.py
#


# -----------------------
# ./zono veran - Fabrizio Verani/016.py
#
import platform

def get_os_info():
    os_name = platform.system()
    os_release = platform.release()

    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

os_info = get_os_info()
print(os_info)

# -----------------------
# ./galvani-vezzi-lidia.zip - Lidia Galvani Vezzi/016.py
#
import platform

def sis_op():
    print(f'OS: {platform.system()}')
    print(f'Release: {platform.release()}')
    print(f'Version: {platform.version()}')
    
sis_op()

# -----------------------
# ./Matteo-Marazzi/016.py
#
import platform

def get_os_info():
    os_name = platform.system()
    os_release = platform.release()

    os_info = f"Sistema Operativo: {os_name}\nRelease: {os_release}"
    return os_info

os_info = get_os_info()
print(os_info)