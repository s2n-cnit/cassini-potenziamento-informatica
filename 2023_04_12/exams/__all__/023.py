

# -----------------------
# ./Baglietto-Luca - Luca Baglietto/023.py
#
if __name__ == "__main__":
    title = input("Inserisci il titolo: ")
    print("Scrivi la canzone")
    with open(title+".txt", "w") as f:
        while True:
            t = input()
            if t == '':
                break
            f.write(t+'\n')
    print("Canzone salvata")

# -----------------------
# ./roggero_costantino/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()

# -----------------------
# ./lin-matteo - Matth6ew/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()

# -----------------------
# ./Zanardi-Leonardo.zip - leonardo zanardi/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./molinari-giorgia/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./porcile-filippo.zip - Filippo Porcile/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./perkola_klaudia - Klaudia Perkola/023.py
#
def salva_testo_canzone(titolo, testo):
    nome_file = titolo + '.txt'
    with open(nome_file, 'w') as file_testo:
        file_testo.write(testo)
    print(f"Testo della canzone '{titolo}' salvato in '{nome_file}'.")

titolo_canzone = input("Inserisci il titolo della canzone: ")
testo_canzone = input("Inserisci il testo della canzone: ")

salva_testo_canzone(titolo_canzone, testo_canzone)

# -----------------------
# ./alessandro-lugaro/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()

# -----------------------
# ./Surdich-Lorenzo - Lorenzo Surdich/023.py
#


# -----------------------
# ./portella-francesco - Francesco Portella/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()

# -----------------------
# ./moretti-christian/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./zumiani-sara.zip - Sara Zumiani/023.py
#


# -----------------------
# ./hu-xuan-di/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./Hunag-Marco.zip - Marco Huang/023.py
#
def salva_canzone():
    # Chiedi all'utente di inserire il titolo e il testo della canzone
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    # Crea il nome del file utilizzando il titolo della canzone
    nome_file = "{}-canzone.txt".format(titolo)

    # Salva il testo della canzone nel file
    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


# Esempio di chiamata alla funzione
salva_canzone()

# -----------------------
# ./zono veran - Fabrizio Verani/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()

# -----------------------
# ./galvani-vezzi-lidia.zip - Lidia Galvani Vezzi/023.py
#


# -----------------------
# ./Matteo-Marazzi/023.py
#
def salva_canzone():
    titolo = input("Inserisci il titolo della canzone: ")
    testo = input("Inserisci il testo della canzone: ")

    nome_file = "{}-canzone.txt".format(titolo)

    with open(nome_file, 'w') as file:
        file.write(testo)

    print("La canzone è stata salvata nel file {}.".format(nome_file))


salva_canzone()