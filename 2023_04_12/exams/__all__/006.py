

# -----------------------
# ./Baglietto-Luca - Luca Baglietto/006.py
#
if __name__ == '__main__':
    numbers = input("Inserisci una lista di numeri: ").split()
    prd = 1
    for n in numbers:
        prd *= int(n)
    print("Il prodotto è", prd)


# -----------------------
# ./roggero_costantino/006.py
#
lista_numeri = [2, 3, 4, 5]

prodotto = 1

for numero in lista_numeri:
    prodotto *= numero

print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./lin-matteo - Matth6ew/006.py
#
def main():
    inputnum = input('inserire lista numeri:')
    lista = inputnum.split()
    p = 1
    for i in range(len(lista)):
        p = p * float(lista[i])
    print(p)

main()

# -----------------------
# ./Zanardi-Leonardo.zip - leonardo zanardi/006.py
#
def mul_list():
    numeri = list(map(float, input('Elenco = ').strip().split(',')))
    risultato = 1
    for n in numeri:
        risultato *= n
        
    print(risultato)
    
mul_list()

# -----------------------
# ./molinari-giorgia/006.py
#
# Definisci la lista di numeri
lista_numeri = [2, 3, 4, 5]

# Inizializza una variabile per il prodotto
prodotto = 1

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Moltiplica il numero corrente al prodotto
    prodotto *= numero

# Stampa il prodotto a schermo
print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./porcile-filippo.zip - Filippo Porcile/006.py
#
# Definisci la lista di numeri
lista_numeri = [2, 3, 4, 5]

# Inizializza una variabile per il prodotto
prodotto = 1

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Moltiplica il numero corrente al prodotto
    prodotto *= numero

# Stampa il prodotto a schermo
print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./perkola_klaudia - Klaudia Perkola/006.py
#
lista = [7, 66, 100, 457, 472]
risultato = 1
for numero in lista:
    if numero != 0:
        risultato *= numero
print("Il risultato della moltiplicazione tra tutti gli elementi della lista è... " + str(risultato))



# -----------------------
# ./ExamFrittoli - Lorenzo Frittoli/006.py
#
def mul_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
mul_list()

# -----------------------
# ./alessandro-lugaro/006.py
#
lista_numeri = [1, 3, 5, 22]

prodotto = 1

for numero in lista_numeri:
    prodotto *= numero

print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./botta-alessio - Alessio Botta/006.py
#
def mul_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
mul_list()

# -----------------------
# ./esercizi informatica minetti giulio - Giulio Minetti/006.py
#
def mul_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
mul_list()

# -----------------------
# ./Surdich-Lorenzo - Lorenzo Surdich/006.py
#
numeri = input("Inserisci una serie di numeri separati da spazi: ").split()

risultato = 1  

for numero in numeri:
    risultato *= int(numero) 
print("Il prodotto dei numeri inseriti è:", risultato)

# -----------------------
# ./todde-lorenzo - lorenzo todde/006.py
#
def main():
    numbers = input("inserisci i numeri che vuoi moltiplicare: ")
    numbers_list = numbers.split()
    sumtot = 1
    for i in numbers_list:
        sumtot = sumtot * float(i)
    print(sumtot)

main()


# -----------------------
# ./dapino-berenice/006.py
#
def main(): 
    list = [2, 9.8 ]
    molt = 1
    for i in list: 
        molt = molt * i 
    
    print("Moltiplicazione: ", molt)

main()

# -----------------------
# ./portella-francesco - Francesco Portella/006.py
#
def main():
    inputnum = input('inserire lista numeri:')
    lista = inputnum.split()
    p = 1
    for i in range(len(lista)):
        p = p * float(lista[i])
    print(p)

main()

# -----------------------
# ./moretti-christian/006.py
#
# Definisci la lista di numeri
lista_numeri = [2, 3, 4, 5]

# Inizializza una variabile per il prodotto
prodotto = 1

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Moltiplica il numero corrente al prodotto
    prodotto *= numero

# Stampa il prodotto a schermo
print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./zumiani-sara.zip - Sara Zumiani/006.py
#
def mult_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
mult_list()

# -----------------------
# ./hu-xuan-di/006.py
#
# Definisci la lista di numeri
lista_numeri = [2, 3, 4, 5]

# Inizializza una variabile per il prodotto
prodotto = 1

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Moltiplica il numero corrente al prodotto
    prodotto *= numero

# Stampa il prodotto a schermo
print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./Hunag-Marco.zip - Marco Huang/006.py
#
# Definisci la lista di numeri
lista_numeri = [2, 3, 4, 5]

# Inizializza una variabile per il prodotto
prodotto = 1

# Utilizza un ciclo for per iterare su ciascun elemento della lista
for numero in lista_numeri:
    # Moltiplica il numero corrente al prodotto
    prodotto *= numero

# Stampa il prodotto a schermo
print("Il prodotto di tutti gli elementi nella lista è:", prodotto)

# -----------------------
# ./zono veran - Fabrizio Verani/006.py
#
def mul_list():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
mul_list()

# -----------------------
# ./galvani-vezzi-lidia.zip - Lidia Galvani Vezzi/006.py
#
def moltiplic():
    nums = list(map(float, input('List: ').strip().split(',')))
    total = 1
    for n in nums:
        total *= n
        
    print(total)
    
moltiplic()

# -----------------------
# ./Matteo-Marazzi/006.py
#
lista_numeri = [1, 3, 5, 22]

prodotto = 1

for numero in lista_numeri:
    prodotto *= numero

print("Il prodotto di tutti gli elementi nella lista è:", prodotto)